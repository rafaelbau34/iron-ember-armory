<%- include('partials/header', { title: title }) %>

<h1><%= title %>: <%= item.name %></h1>

<% if (error) { %>
  <div class="alert alert-danger">
    <%= error %>
  </div>
<% } %>

<p>Are you sure you want to delete the item <strong><%= item.name %></strong>?</p>

<form method="POST" action="" class="form-container">
  <div class="form-group admin-auth">
    <label for="admin_password"><span class="lock-icon">🔒</span> Admin Password Required:</label>
    <input type="password" id="admin_password" name="admin_password" required>
  </div>
  
  <button type="submit" class="btn btn-danger">Confirm Delete</button>
  <a href="/items/<%= item.id %>" class="btn btn-secondary">Cancel</a>
</form>

<%- include('partials/footer') %>
